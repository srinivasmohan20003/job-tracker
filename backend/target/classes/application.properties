# ============================================
# AI Job Application Tracker - Configuration
# ============================================
spring.application.name=job-tracker

# Active Profile (override via env: SPRING_PROFILES_ACTIVE)
spring.profiles.active=dev
