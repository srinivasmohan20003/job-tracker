package com.jobtracker.entity;

/**
 * Enum representing user roles in the system.
 * USER  - Standard user who can manage their own job applications
 * ADMIN - Administrator with elevated privileges
 */
public enum Role {
    USER,
    ADMIN
}
